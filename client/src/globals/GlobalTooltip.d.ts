import React from 'react';
type GlobalTooltipType = {
    children: React.ReactNode;
    text: string;
    type?: string;
};
declare const GlobalTooltip: ({ children, text, type }: GlobalTooltipType) => import("react/jsx-runtime").JSX.Element;
export default GlobalTooltip;
