type InputFormTypes = {
    label: string;
    type: string;
    name: string;
    placeholder?: string;
};
declare const InputForm: ({ label, type, name, placeholder }: InputFormTypes) => import("react/jsx-runtime").JSX.Element;
export default InputForm;
